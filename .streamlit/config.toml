[client]
toolbarMode = "minimal"

# ── Single fixed theme — Streamlit ignores system preference and uses this ────
[theme]
base                     = "dark"
backgroundColor          = "#171717"
secondaryBackgroundColor = "#282828"
textColor                = "#FCFCFC"
primaryColor             = "#B4E817"
font                     = "sans serif"
